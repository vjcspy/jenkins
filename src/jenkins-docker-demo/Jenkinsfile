pipeline {
    agent {
        docker {
            image 'node:16.13.1-alpine'
            args '-u root'
            alwaysPull false
            reuseNode true
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
